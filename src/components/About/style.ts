import styled from "styled-components";

export const Container = styled.section`
    p{
        text-align: justify;
        color: #fff;
        line-height: 2rem;
        padding: 1rem;
        overflow: hidden;
    }
`